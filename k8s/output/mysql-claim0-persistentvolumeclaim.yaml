apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mysql-claim0
  name: mysql-claim0
spec:
  # volumeName: mysql-pv-0
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
